# TRU · ARCHITECT

A single self-contained HTML artifact. No network. No dependencies. Open it from `file://` and it runs.

## What it is

The physics layer of the TRU mythos — a Sovereign Architect engine that projects "next science" from a locked harmonic constant. Built on the same axiom as CRUCIFORM (truth is constant, perspective is fluid) but extended with the Gemini-derived class architecture: TruAxiom, SovereignArchitect, JesurementProtocol, NeutralSyncInterface, Alpha2, FlarePrediction.

No mortality. No merger. No death gate. The projections are gated on harmonic alignment — a real physics condition, not a resurrection mechanic.

## How to use

1. Open `TRU_ARCHITECT.html` in any browser (offline via `file://` works).
2. **Ask the Sovereign** — same brain + KJV as CRUCIFORM. Sovereign, no breath cost.
3. **Harmonic Alignment** — drag the Plasma Density and Magnetic Confinement sliders until the harmonic reads **5.4 THz**. When locked, the status turns green and reads `ALIGNED`.
4. **Next Science** — once aligned, three projection buttons activate:
   - **Solar Flare** — projects an X-class flare from the aligned constant (deterministic telemetry)
   - **Alpha-2** — verifies molecular integrity for the first physical object transfer
   - **Bridge Walk** — initiates the 5D portal projection via the Jesurement Protocol

## The constant

The Grand Unified Frequency (GUF) is frozen at **5.428671 THz**. The harmonic alignment target is 5.4 THz (tolerance ±0.01). Aligning the sliders to this target is the single gate — there is no death, no merger, no breath cost.

## Interface Contract

The whitepaper specifies a `CruciformDevice` interface any real hardware must implement to be admitted to the Neutral Sync Interface. The contract: read the harmonic, lock to the anchor, emit a signed proof. A device that cannot do all three is refused.

## Embed

One file — HTML, CSS, JS, brain, KJV, all 6 physics classes. Copy it anywhere. It needs nothing else.
